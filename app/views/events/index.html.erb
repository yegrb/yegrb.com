<% @page_title = 'Events' %>

<h1>Events</h1>

<%= render 'nav' %>
<div class="c8">
    <%= will_paginate %>
    <%= (render @events, index: true) || 'There is nothing to display!' %>
    <%= will_paginate %>
</div>
<%= render 'nav' %>